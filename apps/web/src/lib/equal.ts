// Compatibility shim: moved to @cognix/mcp-core (docs/monorepo.md Phase 4) so
// extracted MCP logic can deep-compare configs without importing app code.
export { default } from "@cognix/mcp-core/equal";
