// Compatibility shim: moved to @cognix/mcp-types (docs/monorepo.md Phase 3) so
// the extracted MCP schemas can use it without importing app code.
export { tag, type Tagged } from "@cognix/mcp-types";
