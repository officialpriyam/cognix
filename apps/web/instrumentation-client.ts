/**
 * Community build: no client-side analytics.
 *
 * Next.js loads this file automatically on the client. The hosted edition
 * initializes a product-analytics SDK here; this edition deliberately does
 * nothing, so a self-hosted instance reports to no one.
 */
export {};
