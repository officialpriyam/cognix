[package]
name = "cognix"
version = "1.26.0"
description = "Cognix Desktop"
authors = ["Priyx"]
edition = "2021"

[lib]
name = "cognix_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
